# AMD-039 — Register session summary

**Target:** POS REST API Specification §18
**Status:** **Approved** — Story 7.3

`GET /register-sessions/{id}/summary` (`REGISTER_READ` or `REGISTER_OPEN`) returns opening, cash-in, cash-out, cash-sales, and server `expectedCash`.
